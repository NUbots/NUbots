package messages.research.scriptoptimizer;

import "messages/input/Sensors.proto";

message OptimizeScriptResult {

    required uint32 iteration = 1;
    required string metadata = 2;
    repeated input.Sensors sensors = 3;
}
