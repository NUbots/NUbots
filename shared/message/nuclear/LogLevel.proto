syntax = "proto3";

package message.nuclear;

/**
 * Replicates the NUClear::LogLevel enum from NUClear
 */
enum LogLevel {
    UNKNOWN = 0;
    TRACE   = 1;
    DEBUG   = 2;
    INFO    = 3;
    WARN    = 4;
    ERROR   = 5;
    FATAL   = 6;
}
