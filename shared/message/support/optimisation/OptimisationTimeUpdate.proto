syntax = "proto3";

package message.support.optimisation;

message OptimisationTimeUpdate {
    double sim_time   = 1;
    double real_time  = 2;
    double sim_delta  = 3;
    double real_delta = 4;
    double rtf        = 5;
}
