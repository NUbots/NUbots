syntax = "proto3";

package message.support.optimisation;

message OptimisationTimeUpdate {
    double real_time  = 1;
    double sim_delta  = 2;
    double real_delta = 3;
    double rtf        = 4;
}
