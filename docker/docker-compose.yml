version: '3'
services:
  clang-format:
    image: nubots:clang_format
    build:
      context: .
      dockerfile: clang_format.Dockerfile
    volumes:
      - .:/home/nubots/NUbots
  nuc7i7bnh:
    image: nubots:nuc7i7bnh
    build:
      context: .
      dockerfile: nubots.Dockerfile
      args:
        platform: nuc7i7bnh
    volumes:
      - .:/home/nubots/NUbots
  generic:
    image: nubots:generic
    build:
      context: .
      dockerfile: nubots.Dockerfile
      args:
        platform: generic
    volumes:
      - .:/home/nubots/NUbots
