# Find ALSA for eSpeak
FIND_PACKAGE(ALSA REQUIRED)

# Find the eSpeak Library
FIND_PACKAGE(eSpeak REQUIRED)

# Build our NUClear module
NUCLEAR_MODULE(INCLUDES ${ALSA_INCLUDE_DIRS} ${ESPEAK_INCLUDE_DIRS}
               LIBRARIES ${ALSA_LIBRARIES} ${ESPEAK_LIBRARIES})
