# Find DC1394 library
FIND_PACKAGE(Spinnaker REQUIRED)

#NUCLEAR_MODULE(INCLUDES ${dc1394_INCLUDE_DIRS}
NUCLEAR_MODULE(INCLUDES ${Spinnaker_INCLUDE_DIRS}
               LIBRARIES ${Spinnaker_LIBRARIES})
