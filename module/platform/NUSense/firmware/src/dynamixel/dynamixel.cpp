#include "dynamixel.hpp"

#include <Arduino.h>

namespace dynamixel {

    void setup() {
        // TODO set up each of the serial ports something like this:
        // HardwareSerial dxl_serial1(rx_pin, tx_pin, rts_pin, cts_pin);
        // HardwareSerial dxl_serial2(rx_pin, tx_pin, rts_pin, cts_pin);
        // HardwareSerial dxl_serial3(rx_pin, tx_pin, rts_pin, cts_pin);
        // HardwareSerial dxl_serial4(rx_pin, tx_pin, rts_pin, cts_pin);
        // HardwareSerial dxl_serial5(rx_pin, tx_pin, rts_pin, cts_pin);
        // HardwareSerial dxl_serial6(rx_pin, tx_pin, rts_pin, cts_pin);
    }

    void loop() {
        // TODO read/write from each of the serial ports
    }

}  // namespace dynamixel
