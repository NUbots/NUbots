# Controls the minimum log level that NUClear log will display
log_level: INFO

# General playing tuning values for approaching ball
max_translational_velocity_magnitude: 0.02
min_translational_velocity_magnitude: 0.01
max_angular_velocity: 0.15
min_angular_velocity: -0.15
acceleration: 0.001 # maximum increment in walk command velocity
approach_radius: 1 # region around ball to begin decelerating

# Rotate on the spot tuning values
rotate_velocity: 0.2
rotate_velocity_x: 0.0
rotate_velocity_y: 0.0

# Rotate around ball tuning values
pivot_ball_velocity: 0.2
pivot_ball_velocity_x: 0.0
pivot_ball_velocity_y: -0.2
