# Controls the minimum log level that NUClear log will display
log_level: INFO

servo_gain: 30

mode: TUNE # IK, KEEP_LEVEL (IK + keep foot orientated with ground), TUNE (Iteratively tune P gain to improve tracking)
