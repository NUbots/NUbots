
SET(CUDA_SEPARABLE_COMPILATION ON)
FIND_PACKAGE(CUDA REQUIRED)

# Build our NUClear module
NUCLEAR_MODULE(LANGUAGE "CPP"
               INCLUDES ${CUDA_INCLUDE_DIRS} ${CUDA_TOOLKIT_ROOT_DIR}
               LIBRARIES ${CUDA_LIBRARIES})
